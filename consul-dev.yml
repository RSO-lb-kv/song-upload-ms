config:
  key: song-upload
consul:
  host: localhost
  port: 8500
  discoveryHost: localhost
  service:
    id: song-upload
    name: song-upload
    port: 3002
    tags: ['v1.0.1']
  healthCheck:
    timeout: 1s
    interval: 10s
    route: /health
  maxRetry: 5
  retryInterval: 5000
