host: 127.0.0.1
port: 8500
config:
  key: song.upload

consul:
  service:
    id: song-upload
    name: song-upload
    port: 3002
    tags: ['v1.0.1']
  healthCheck:
    timeout: 1s
    interval: 10s
    route: /health
  maxRetry: 5
  retryInterval: 5000
